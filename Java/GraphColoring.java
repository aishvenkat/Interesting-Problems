import java.util.*;

class GraphColoring{
static int noOfColors = 1;
int noOfVertices=0;
int[] Colors;
ArrayList<ArralyList<Integer>> AdjList = new ArrayList<ArrayList<Integer>>();

public void initialize(int noOfVertices){
 this.noOfVertices = noOfVertices;
 Colors = new int[noOfVertices];
 for(int i = 0; i < noOfVertices;i++){
 }
}
public void insert(int start, int end){
 
}
}
